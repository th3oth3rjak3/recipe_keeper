name: Release

on:
  push:
    tags:
      - "v*" # Runs when a tag starting with "v" is pushed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y dpkg-dev debhelper libsqlite3-dev

      - name: Set Up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: client/package-lock.json

      - name: Install Node.js Dependencies
        run: npm install
        working-directory: ./client

      - name: Build Rust and Frontend
        run: |
          # Build frontend with Vite and output to static folder
          npm run build
          cargo build --release

      - name: Verify Build Outputs
        run: |
          ls -lah target/release
          ls -lah static
          ls -lah Rocket.toml || echo "No Rocket.toml found"

      - name: Prepare Package Directory
        run: |
          APP_NAME="recipe_keeper"
          VERSION="${GITHUB_REF#refs/tags/v}"

          # Create the output directory structure with everything in one folder
          mkdir -p package/$APP_NAME

          # Copy Rust binary
          cp target/release/$APP_NAME package/$APP_NAME/

          # Copy entire static folder to package directory
          cp -r static package/$APP_NAME/

          # Copy Rocket.toml if it exists
          if [ -f Rocket.toml ]; then
            cp Rocket.toml package/$APP_NAME/
          fi

          # Verify the contents of the package
          ls -lah package/$APP_NAME

      - name: Package .deb
        run: |
          APP_NAME="recipe_keeper"
          VERSION="${GITHUB_REF#refs/tags/v}"

          # Create control file for .deb metadata
          mkdir -p package/DEBIAN
          cat <<EOF > package/DEBIAN/control
          Package: $APP_NAME
          Version: $VERSION
          Section: utils
          Priority: optional
          Architecture: amd64
          Depends: libc6 (>= 2.28), libsqlite3-dev
          Maintainer: Jake Hathaway <jake.d.hathaway@gmail.com>
          Description: Recipe Keeper - A simple web-based recipe manager built with Rust, Rocket, and SQLite.
          EOF

          # Build the .deb package
          dpkg-deb --build package ${APP_NAME}_${VERSION}_amd64.deb

      - name: Upload .deb Artifact
        uses: actions/upload-artifact@v4
        with:
          name: recipe_keeper-deb
          path: recipe_keeper_*_amd64.deb

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: recipe_keeper_*_amd64.deb
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
